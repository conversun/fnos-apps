# AutoBangumi for fnOS

基于 RSS 的全自动追番下载整理工具，支持 Mikan 等数据源，自动下载并重命名为 Plex/Jellyfin 兼容格式。

- 默认端口: 7892
- 默认账号: admin / adminadmin
- 需要配合 qBittorrent 使用

## Local Build

```bash
cd apps/auto-bangumi && bash ../../scripts/build-fpk.sh . app.tgz
```
