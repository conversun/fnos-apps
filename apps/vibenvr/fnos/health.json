{
    "type": "http",
    "path": "/",
    "expect_status": [200, 301, 302, 401, 403],
    "startup_timeout_seconds": 180,
    "post_install_warmup_seconds": 15,
    "skip_arch": [],
    "note": "Docker stack exposes only the frontend web UI on TRIM_SERVICE_PORT."
}
