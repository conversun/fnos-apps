#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

DATA_DIR="$APP_DATA_DIR"
mkdir -p "$DATA_DIR"

cd "$APP_DIR" || exit 1
exec ./msf serve --config "$DATA_DIR" --host 0.0.0.0 --port "${TRIM_SERVICE_PORT:-7777}"
