# Mail Archiver for fnOS

Mail Archiver 是用于归档、搜索和导出 IMAP / Microsoft 365 邮件的自托管系统。本 fnOS 包使用 Docker 模式运行 Mail Archiver 应用容器和 PostgreSQL 数据库容器。

## Local Build

```bash
cd apps/mail-archiver && ./update_mail-archiver.sh
```
