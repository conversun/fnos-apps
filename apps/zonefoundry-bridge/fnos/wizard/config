[
    {
        "stepTitle": "ZoneFoundry Bridge",
        "items": [
            {
                "type": "tips",
                "helpText": "ZoneFoundry Bridge 以客户端模式运行，将家中 Sonos 音箱接入 ZoneFoundry 云端中继。<br><br>该容器使用 host 网络模式（Sonos LAN UPnP/SSDP 发现需要），不映射独立端口。"
            },
            {
                "type": "tips",
                "helpText": "<b>无需先打开 iOS app</b>：直接点 <b>下一步 → 启动</b>。容器首次启动会在日志里打印一个 6 位 <b>Setup code</b>（例如 <code>ZF-LMNO</code>）。<br><br>之后打开 ZoneFoundry iOS app → 更多 → 桥接 → 右上 <b>+ → 输入设置码</b>，把那串码输入即可。桥接会在 ~5 秒内自动配对完成，凭证持久化到 <code>${TRIM_PKGVAR}/data</code>，重启 fnOS 后无需重新输入。"
            }
        ]
    }
]
