# Next AI Draw.io for fnOS

Next AI Draw.io 是一个将大语言模型与 draw.io 图表编辑结合的 Web 应用，可通过自然语言生成、修改和增强流程图、架构图等图表。

本 fnOS 包使用 Docker 镜像 `ghcr.io/dayuanjiang/next-ai-draw-io:latest`，默认 Web 端口为 `3007`，容器内部端口为 `3000`。
